/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['parthsarthibuilder.com', 'res.cloudinary.com','example.com'], // Added Cloudinary domain here
  },
};

export default nextConfig;
